<%= @review.comment %>
<%= link_to "Edit", "/books/#{@book.id}/reviews/#{@review.id}/edit" %>
<%= link_to "Delete", "/books/#{@book.id}/reviews/#{@review.id}", {method: :delete, data: {confirm: "Are you sure you want to delete this review?"}} %>